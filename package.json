{
  "name": "travel-crm",
  "version": "1.0.0",
  "description": "Travel Agency CRM - Trek & Pilgrimage Management System",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "start \"Backend\" cmd /k npm run dev:backend && start \"Frontend\" cmd /k npm run dev:frontend",
    "seed": "npm run db:seed --prefix backend",
    "db:push": "npm run db:push --prefix backend",
    "db:studio": "npm run db:studio --prefix backend"
  },
  "private": true,
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}
