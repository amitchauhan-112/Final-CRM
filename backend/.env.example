# Supabase Postgres — DATABASE_URL is the pooled connection (port 6543,
# ?pgbouncer=true) used at runtime; DIRECT_URL is the direct connection
# (port 5432) needed for `prisma migrate`.
DATABASE_URL="postgresql://postgres:password@localhost:5432/travel_crm"
DIRECT_URL="postgresql://postgres:password@localhost:5432/travel_crm"

JWT_SECRET="change-this-to-a-strong-random-secret-min-32-chars"
JWT_EXPIRES_IN="7d"
PORT=5000
NODE_ENV=production

# Set to your Vercel frontend URL after deploying (unnecessary once
# frontend+backend share one Vercel project/domain)
FRONTEND_URL="https://your-app.vercel.app"

# Supabase Storage (S3-compatible) — used for file uploads instead of local disk
SUPABASE_S3_ENDPOINT="https://<project-ref>.storage.supabase.co/storage/v1/s3"
SUPABASE_S3_REGION="us-east-1"
SUPABASE_S3_ACCESS_KEY_ID="your-supabase-s3-access-key-id"
SUPABASE_S3_SECRET_ACCESS_KEY="your-supabase-s3-secret-access-key"
SUPABASE_STORAGE_BUCKET="crm-uploads"

# Shared secret the external scheduler (cron-job.org / GitHub Actions) sends
# as the x-cron-secret header when triggering /api/cron/* routes
CRON_SECRET="change-this-to-a-strong-random-secret-min-32-chars"

# Meta WhatsApp Business API
WHATSAPP_VERIFY_TOKEN="your-whatsapp-verify-token"
WHATSAPP_ACCESS_TOKEN="your-whatsapp-access-token"
WHATSAPP_PHONE_NUMBER_ID="your-phone-number-id"
WHATSAPP_BUSINESS_ACCOUNT_ID="your-business-account-id"

# Meta Instagram / Graph API
META_APP_ID="your-meta-app-id"
META_APP_SECRET="your-meta-app-secret"
META_PAGE_ACCESS_TOKEN="your-page-access-token"
INSTAGRAM_VERIFY_TOKEN="your-instagram-verify-token"

# Email (Nodemailer)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
FROM_EMAIL="noreply@travelagency.com"
FROM_NAME="Travel Agency CRM"
