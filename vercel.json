{
  "buildCommand": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build && npm ci --include=dev && npm --prefix backend ci --include=dev && npm --prefix backend run db:generate",
  "installCommand": "echo 'Install handled by buildCommand'",
  "outputDirectory": "frontend/dist",
  "regions": ["syd1"],
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
