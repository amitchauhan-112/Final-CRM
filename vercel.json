{
  "buildCommand": "npm --prefix frontend install && npm --prefix frontend run build && npm install && npm --prefix backend install && npm --prefix backend run db:generate",
  "installCommand": "echo 'Install handled by buildCommand'",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
