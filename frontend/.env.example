# Set this to your backend's public URL (no trailing slash, no /api suffix —
# that's appended automatically). On Vercel, set this as a Project ->
# Environment Variable rather than committing a real value here.
# Example: VITE_API_URL=https://crm.bhatko.in
VITE_API_URL=
